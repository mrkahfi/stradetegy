{% extends '::base.html.twig' %}

{% block body -%}
    <h1>MemberARB list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Successful</th>
                <th>Processed</th>
                <th>Amount</th>
                <th>Chargedate</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('arb_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.successful }}</td>
                <td>{{ entity.processed }}</td>
                <td>{{ entity.amount }}</td>
                <td>{% if entity.chargeDate %}{{ entity.chargeDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('arb_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('arb_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('arb_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
